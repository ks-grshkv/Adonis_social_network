<!DOCTYPE html>
<html lang="en">
   <head>
      <link 
         href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" 
         rel="stylesheet"
         integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" 
         crossorigin="anonymous">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Hey, what's up?</title>
      {{--  @entryPointStyles('app')  --}}
      {{--  @entryPointScripts('app')  --}}
   </head>
   <header>
      <nav class="navbar navbar-light" style="background-color: #dddddd">
         <div class="container">
            <ul class="nav nav-pills">
            <a href="{{route('NewsController.index')}}">ShitterGram</a>
            </ul>
            <ul class="nav nav-pills">
               <div class="action-buttons">
                  @if(auth.isAuthenticated == false)
                  <a href="{{route('AuthController.login')}}" class="btn btn-primary"> Login </a>
                  @else
                  <a href="{{route('NewsController.make_news')}}" class="btn btn-primary"> Post Something </a>
                  @endif
            </ul>
            <ul class="nav nav-pills">
            @if(auth.isAuthenticated)
            <form action="{{route('AuthController.logout')}}" method="post">
                {{ csrfField() }}
            <button class="btn btn-primary" type="submit"> Sign Out </button>
            </form>
            @endif
            </ul>
            </div>
         </div>
      </nav>
   </header>
   <body>
      @section('content')
      @endsection
   </body>
</html>